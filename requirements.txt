# Core agent framework
langchain==0.3.25
langchain-anthropic==0.3.15
langchain-community==0.3.24

# LLM — Claude via Anthropic SDK (must be >=0.52.0 to match langchain-anthropic 0.3.15)
anthropic==0.52.0

# Web search — Perplexity Search API returns structured results (title, URL, snippet, date)
perplexityai==0.32.1

# Notion integration
notion-client==2.3.0

# Observability — LangSmith traces every agent run
langsmith==0.3.45

# Environment variable management
python-dotenv==1.1.0

# Testing
pytest==8.3.5
pytest-asyncio==0.26.0

# Jupyter notebooks (for Stage 1 experiments)
jupyter==1.1.1
ipykernel==6.29.5
