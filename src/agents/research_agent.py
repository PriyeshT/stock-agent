# Stage 3 — ReAct research agent.
# Unlike a chain (fixed steps), an agent decides its own next step.
# ReAct = Reason + Act: the LLM reasons about what to do, then calls a tool,
# then reasons again based on the result — looping until it has an answer.
# This agent researches all 9 watchlist stocks and returns signals + summaries.

# TODO (Stage 3): implement research agent
