# Stage 4 — Notion read/write operations.
# Reads the portfolio table (to check positions and status).
# Writes the weekly brief as a new Notion page.
# Notion's API works with "blocks" — every paragraph, heading, and table
# is a separate block you assemble like LEGO.

# TODO (Stage 4): implement Notion read/write client
