[pytest]
# Where to find tests
testpaths = tests

# Show each test name as it runs (not just a dot)
addopts = -v

# Required for async tests (used when testing agents with async LangChain calls)
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
